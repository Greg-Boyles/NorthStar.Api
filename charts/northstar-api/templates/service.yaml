apiVersion: v1
kind: Service
metadata:
  name: {{ include "northstar-api.fullname" . }}
  labels:
    {{- include "northstar-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "northstar-api.selectorLabels" . | nindent 4 }}
